A COMMON/DATABASE/PGjpaManagerFactory -> persistence name=testdb a puName
---------------------
RESTEDClient

boundary/rest/Jtar -> posem el path al final de la url (api/jta_mytable)

amb el get fem una petició a la base de dades al repositori que ha creat

al curl et surt la comanda x fer la petició al cmd
 
---------------------------------------------
jakartaconfig te una api x fer peticionsd e dades, la ruta ha de ser diferent a la de les web

comentar linea securityKey de MyTableJTARepositoryResource i reserService

utilitzar clean and build i run x iniciar la app
-------------------------------------------------
el jtar representa les clases de java 
-------------------
fer un parell de models relacionat basics, repositoris i servei, giar-se amb els exemples de myTable

a la carpeta data/model

provar lo del get i create

x importar models amb asistent de netbeans , entityclases from databases

asobre de model clic dret 
 
seleccionem bdd (testdb),pase,seguent,dos primeres caselles,seguent,marcar tot,colletion type->list,finish --> crea un obj amb els camps definitd

-------------------------------------
buscar que es lo de @produces i @consumes
que es el head de restclient
---------------------

