HEAD, como el GET, pero no devuelve el cuerpo (body) de la respuesta, solo los encabezados (headers).

xdata -> cuan el servidor fa tot lo de transaccions, fa rollback, x persistencia de dades

@produces es x entrada, tipus de dada i @consumes de sortida
Al target es veu on es pot aplicar la anotació

es pot posar en general a la clase i a les funcions x separat

al header content-type i el tipus de dada (json i eso)

 @pathparam --> parametre definit com a part de url separat x /
 @queryparam --> cal posar id=1  es separe per ?
 
 la difernacia es la forma de posar-ho al URL
 path: /1 query: /id=1
 ----------------------------------
  els metodes no poden tindre la mateixa definició, si necesitem 2 metodes que entrin un int x ex cal cambiar el path i posar algo davant, com /nom/id
  
---------------------

-restserviceLog
qualifer-type
x enllaçar 2 clases
genera fitxer(logs) de entrada i sortida cada cop que es fa petició al servei

-securityKeyAuth
authorization al header = token x permisos
authkey a la clase defineix els tokens

 al response.java hi han els errors definits
 
 intentar millorar el metode a common/rest-
 /seckey 
 
 buscar lo de la prioritat de la classe
